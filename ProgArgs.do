// Demonstration of defining a program, passing arguments,
// referencing and displaying the arguments passed.

set more off
clear all

program funfoo
	di "0 `0'"
	di "1 `1'"
	di "2 `2'"
	di "3 `3'"
	di "4 `4'"
	di "5 `5'"
	di "6 `6'"
	di "* `*'"
end

funfoo And here we pass six arguments.
